fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0
pydantic>=2.0.0
# anthropic removed — memory gate now uses SchemaCaptureClient (static, LLM-free).
# Re-add `anthropic>=0.40.0` only if AnthropicClient is re-enabled in memory/llm_client.py.
# chromadb deferred to the enterprise VDB phase — current VDB is pure JSON, no new deps.

# Gate tools
ruff>=0.4.0
mypy>=1.9.0
bandit>=1.7.8
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
